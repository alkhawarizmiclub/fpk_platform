import PageLayout from "../layouts/PageLayout";

const PresentationPage = ({ page_title }) => {
    return (
        <PageLayout page_title={page_title}>
            PresentationPage
        </PageLayout>
    );
}

export default PresentationPage;